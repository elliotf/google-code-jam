Warning: Most old problems are broken due to changes I've made to the repo.

Solutions for google code jam problems

* You need to have node and npm
* You need to have libgmp-dev for bigint (see https://github.com/substack/node-bigint)
* You need to `npm install` dependencies


